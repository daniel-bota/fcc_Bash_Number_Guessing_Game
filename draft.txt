CREATE TABLE users(
  user_id SERIAL PRIMARY KEY,
  username VARCHAR(22) NOT NULL);

CREATE TABLE games(
  game_id SERIAL PRIMARY KEY,
  guesses INT);

ALTER TABLE games ADD COLUMN user_id INT;
ALTER TABLE games ADD FOREIGN KEY(user_id) REFERENCES users(user_id);
ALTER TABLE users ADD UNIQUE(username);

ALTER TABLE games ALTER COLUMN user_id SET NOT NULL;
ALTER TABLE games ALTER COLUMN guesses SET NOT NULL;